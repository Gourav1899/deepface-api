services:
  - type: web
    name: deepface-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: MONGO_URI
        sync: false
      - key: GOOGLE_SERVICE_ACCOUNT_JSON
        sync: false
    healthCheckPath: /health
    # Auto-deploy on push
    autoDeploy: true
